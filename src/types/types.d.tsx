export interface SideBarMenuItem {
    id: string;
    label:string;
    icon: string;
    url: string;
}

export interface SideBarMenuCard{
    id: string;
    displayName:string;
    photoUrl: string;
    title: string;
    url:string;
}